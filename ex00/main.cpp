#include "easyfind.hpp"
#include <iostream>
#include <vector>


int main()
{
    // std::vector<int> vect;
    // vect.push_back(11);
    // vect.push_back(13);
    // vect.push_back(111);
    // vect.push_back(133);
    // std::vector<int>::iterator it;
    // it = vect.begin();
    // // while(it != vect.end())
    // // {
    // //     std::cout << " " << *it << std::endl;
    // //     it++;
    // // }

    // for (it = vect.begin() ; it != vect.end(); it++)
    // {
    //     std::cout << " " << *it << std::endl;
    // }

    return 0;
}